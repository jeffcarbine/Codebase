export enum icon {
    building = 'building',
    driversLicense = 'driversLicense',
    passport = 'passport',
    user = 'user',
    users = 'users',
}

// Not sure I like this
export enum mappedIcon {
    'Driver\'s License' = icon.driversLicense,
    'Passport' = icon.passport,
    'Business' = icon.building,
}