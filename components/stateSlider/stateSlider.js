import { addEventDelegate } from "/periodic/scripts/eventdelegate/_eventdelegate.js";

const stateSliderButton = (button) => {
  const target = document.querySelector();
};

addEventDelegate("click", ".stateSlider button", stateSliderButton);

const stateSliderElement = (element) => {};

addEventDelegate("click", ".stateSlider .element", stateSliderElement);

export const initStateSliders = () => {
  const stateSliders = document.querySelectorAll(".stateSlider");

  stateSliders.forEach((stateSlider) => {
    // we need to
  });
};
